namespace ServiceTests
{
    [TestClass]
    public sealed class FlightApiServiceTests
    {
        [TestMethod]
        public void GetFlight()
        {
            // Arrange
            //var flightApiService = new FlightApiService();
            //var flightId = 1; // Assuming you have a flight with ID 1 in your mock data
            //// Act
            //var result = flightApiService.GetFlight(flightId);
            //// Assert
            //Assert.IsNotNull(result);
            //Assert.AreEqual(flightId, result.Id);
            //Assert.AreEqual("Mock Airline", result.Airline);
            //Assert.AreEqual(100, result.SeatsAvailable);
        }
    }
}
